#include<iostream>

using namespace std;

string find(string str, string findstring)
{
    int size;
    size = str.length();
    for(int i = 0; i < size; i++)
    {
        if(str[i] == findstring)
        {

        }
    }
}
int main()
{
    string str;
    int size;

    cout<<"enter string: ";
    getline(cin , str);
    size = str.length();
    
    for

}